import { View, Text } from 'react-native';

export default function PlanScreen() {
    return (
        <View style={{ flex: 1 }}>
            <Text>여행 계획 화면</Text>
            {/* 여행 계획 관련 컴포넌트 삽입 */}
        </View>
    );
}
